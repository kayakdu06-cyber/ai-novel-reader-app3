# 织卷工作汇报 37：极简创建页面

> 日期：2026-08-02  
> 任务：TASK-033 / E3.4  
> 状态：功能、双模拟器、适配和视觉验收完成

## 本次完成

- 有已保存的当前连接时，App 再次启动会直接进入“织一本新书”，连接列表降为按需管理入口；
- 默认路径只要求一句故事设想。题材可以不选，篇幅默认“中篇”，呈现默认“均衡”，用户无需理解专业创作参数；
- 常用题材直接显示，其他题材放进“更多题材”；题材目录已从界面代码中分离并携带 schema 版本，为后续版本化预设留出稳定接口；
- 内容呈现使用“留白 / 均衡 / 细写”三档克制命名，默认页面不出现直白尺度标签；
- 当前连接只显示用户命名和模型，并可进入管理页，不显示原始密钥、尾四位或内部引用；
- 页面提交结构化 `MinimalBookDraft`，包含设想、可空题材、篇幅、呈现档位和预设目录版本。

## 产品边界

TASK-033 只完成低操作输入和结构化交接，不提前冒充后续能力。点击“开始织卷”后，当前版本会明确提示“尚未发送给模型”；不会写入一部伪造的小说，也不会调用真实 API。

- 高级人物、世界和其他可选资料：TASK-034；
- 外部呈现档位到内部精确维度的版本化映射：TASK-035；
- 标准化与不可变创建快照：TASK-036；
- 费用与用量确认：TASK-037；
- 实际生成状态机：E4。

这也修正了原需求中的一处冲突：默认创作页可以展示三个隐蔽命名的呈现档位，但成人内容的精确内部控制不在默认页直接暴露，仍由后续映射和创作宪法共同约束。

## 视觉与易用性

本轮使用既有 UI/UX 设计规范复核了实现，最终沿用已确认的暖纸、深墨、浅赭选中态和单点橙色，没有采用搜索结果中与产品不符的紫色 AI 风格。具体落实为：

- 单一纵向滚动容器，避免嵌套滚动和小屏内容消失；
- 所有字段有常驻文字标签，不只依赖占位符；
- 内容宽度上限 600dp，兼顾手机横竖屏；
- 主要触摸目标不小于 48dp，开始按钮为 56dp；
- 空设想时按钮禁用，提交后显示真实阶段反馈；
- 支持浅色、深色、200% 字号、横屏和约 375dp 宽窄屏。

浅色竖屏截图：`D:\gptuser\cache\temp\zhijuan-task033-create.png`。

## 验证结果

- 新增 Compose 极简创建测试 6 项；
- API 35 标准竖屏：6/6；
- API 35 200% 系统字体：6/6；
- API 35 横屏：6/6；
- API 35 约 375dp 宽窄屏：6/6；
- API 30 首次说明、向导、安全接线、连接管理与创建组合：22/22；
- API 35 App 模块最终全量：29/29；
- 表单在重建后保留设想、题材、篇幅和呈现档位；
- 真实 API 调用 0，实体手机安装、写入和设置变更 0。

## 下一步

自动进入 TASK-034 高级创建折叠区。默认路径继续保持一句话可开始，只有用户主动展开时才显示人物、世界和其他可选资料；输入状态需在折叠、旋转和进程重建后保持。
