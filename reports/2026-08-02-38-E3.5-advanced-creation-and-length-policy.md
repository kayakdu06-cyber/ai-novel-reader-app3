# 织卷工作汇报 38：高级创建与篇幅规则

> 日期：2026-08-02  
> 任务：TASK-034 / E3.5  
> 状态：功能、双模拟器、适配、视觉和文档验收完成

## 本次完成

- 创建页新增“再补充一点（可选）”折叠区，默认不展开，不增加一句话创建路径的必填操作；
- 高级区只使用五组自然语言输入：人物与关系、世界与背景、叙事与文风、希望保留、不想出现，全部可留空；
- 折叠后显示已填写项数，再展开内容不丢；Activity 重建、旋转和临时进入连接管理再返回时，设想、选择、长篇章数、高级输入和展开状态均保留；
- 提交结果使用独立结构化字段并去除首尾空白，为后续标准化和不可变快照提供稳定输入；
- 默认页面继续隐藏直白尺度名称；18 岁以上提示只出现在用户主动展开的人物帮助文案内；
- 采用单一 LazyColumn、显式标签、就地错误说明和 48/56dp 触摸目标，没有加入装饰性动效或额外确认步骤。

## 篇幅规则修正

用户指出原有篇幅过少后，本轮将篇幅从展示文案提升为版本化产品规则：

| 模式 | 规划下限 | 默认/用户目标 | 页面行为 |
|---|---:|---:|---|
| 短篇 | 80 章 | 80 章 | 直接选择，无额外输入 |
| 中篇 | 300 章 | 300 章 | 默认选择，无额外输入 |
| 长篇 | 301 章 | 用户必填 301–10,000，系统不预填默认值 | 选择后才显示数字字段；空值或非法值禁用开始按钮 |

草稿现在明确携带 `minimumChapterCount`、`targetChapterCount` 和 `lengthPolicySchemaVersion=1`。领域篇幅模式收束为 `SHORT/MEDIUM/LONG`，长篇用户目标保存在目标字段，不再保留语义重复的 `CUSTOM`。首版 10,000 章上限与现有万章目录压力验证一致，主要用于拦截误输，未来可通过新规则版本调整。

## 测试与问题修正

- 创建页 Compose 测试 7 项，覆盖中篇默认 300、短篇 80、长篇 300 非法/888 合法、长篇 999 重建恢复、五组高级输入、折叠保持、触摸目标、浅色截图和深色可达；
- API 35：标准竖屏、200% 字号、横屏、约 375dp 窄屏分别 7/7；
- API 30：首次说明、连接向导、安全接线、连接列表与创建组合 24/24；
- API 35 App 模块最终全量 31/31，0 失败、0 错误、0 跳过；
- 全量连续测试曾暴露旧连接向导测试通过完整 Activity 替换并依赖触摸坐标，模拟器高负载时会误报点击未发生。本轮将其改为隔离页面容器、语义点击和跨线程可见结果；创建页与向导连续 11/11 后，最终全量通过；
- 横屏首轮测试在模拟器旋转未稳定时出现页面未启动，等待系统稳定后同一 7 项全部通过；业务实现无需修改；
- 真实 API 调用 0；所有设备命令只指定 `emulator-5554` 或 `emulator-5556`；实体设备安装、写入和设置变更 0。

## 视觉证据

浅色竖屏截图：`D:\gptuser\cache\temp\zhijuan-task034-create.png`。

截图已经人工检查：暖纸背景、深墨正文、浅赭选中态和橙色单点强调保持一致；短篇“最低 80 章”和默认中篇“最低 300 章”清晰可见；页面没有密钥、尾四位或 secret 引用。

本轮沿用 UI/UX 设计规则中的渐进披露、常驻字段标签、就地校验、状态恢复、触摸目标和多尺寸适配要求；它直接影响了高级区默认折叠、长篇字段按需显示以及错误不弹额外对话框的实现。

## 尚未越权完成

- 当前“开始织书”仍只产生内存中的结构化草稿，不写入真实书、不调用模型；
- 篇幅下限/目标/schema v1 的数据库迁移、约束和不可变快照由 TASK-036 完成；
- 费用估算必须按目标章数进入 TASK-037，不能在确认前调用模型；
- 分卷/窗口式分章规划和真实逐章生成属于 E4/E5，不用一次超长请求冒充 80/300/自定义长篇能力。

## 下一步

自动进入 TASK-035 内容呈现外部/内部映射：保持“留白 / 均衡 / 细写”的隐蔽名称，把内部场景展开程度、身体与感官连续性、避免淡出等维度做成版本化、可测试的纯领域映射，不增加默认页面操作。
