# 纯文本模型任务包模板

> 用法：Sol 在把一项工作交给 DeepSeek 前复制本模板，删除提示语并填完整。任务包必须能够脱离历史对话独立理解。

## 任务身份

- 任务 ID：`TASK-___ / 子任务名称`
- 仓库根目录：`D:\gptuser\projects\ai-novel-reader-app3`
- 基准分支与 HEAD：
- 当前未提交改动：
- 执行模型：DeepSeek（纯文本）

## 运行预算

- 推理等级：`max`（用户 2026-08-04 的持续指令；除非用户另行变更，不得降低）
- 最长运行时间：通常 15～30 分钟；此前同一窄任务在 30 分钟因正常推理超时且无其他阻塞时，可放宽到最多 45 分钟（填写理由）
- 累计 Token 上限：无（用户 2026-08-08 的持续授权；仍受硬超时和单任务锁约束）
- 预计读取文件数与明确清单：
- 预计执行命令/测试数：
- 提前停止条件：权限阻塞、范围需要扩张、重复失败、预算不足或出现未声明的高风险改动

## 目标

用一至三句话描述本次必须交付的可验证结果。

## 当前现场与已有 WIP

- 已存在的实现：
- 已存在的测试：
- 已知失败或缺口：
- 必须延续、不得从零重写的部分：

## 必读资料

1. `AGENTS.md`
2. `docs/24-AI-DEVELOPMENT-PROTOCOL.md`
3. `docs/ai/CURRENT-CONTEXT.md`
4. 当前任务对应的编号文档和具体章节：
5. 必须阅读的源代码和测试：

除上述清单和代码直接引用外，不得递归扫描整套文档、历史会话、备份或无关模块；需要扩展读取范围时先在回交中说明。

## 范围

允许修改：

- 文件/模块：

明确不在范围：

- 文件/模块/产品行为：

## 不可破坏的约束

- 项目隔离：不得访问或修改其他项目副本。
- 多模态：不得推测图片、截图、录屏或视觉结果；列入“需要 Sol 处理”。
- 安全与隐私：
- 状态机与幂等：
- 数据库与事务：
- 联网与费用：默认不调用真实 API、不产生费用。
- 兼容性与构建基线：
- 需要保留的用户/未提交改动：

## 实施要求

1. 需要完成的代码或文档工作：
2. 需要新增或更新的测试：
3. 错误、并发、恢复和 replay 场景：
4. 不允许采用的捷径：

## 验收标准

- [ ] 行为标准：
- [ ] 失败路径：
- [ ] 安全/隐私：
- [ ] 向后兼容：
- [ ] 文档同步：

## 验证命令

```powershell
# 填写最小相关测试
```

```powershell
# 需要时再运行统一离线门禁
scripts/verify-build.ps1 -Offline
```

未运行的验证必须在回交中写明原因，不能写成通过。

## 回交格式

请严格按以下标题返回：

1. `完成内容`
2. `修改文件`
3. `验证`
4. `未完成/风险`
5. `需要 Sol 处理`
6. `假设`

不要宣布整个 TASK 完成，也不要更新正式完成状态；由 Sol 根据差异和测试证据确认。
