# 费用确认页规范

> 页面任务：从已经冻结并回读的新书快照展示篇幅和模型，诚实说明价格可用性；在预算与生成执行器接入前，不发出模型请求。继承 `../MASTER.md`，本页规则优先。

## 1. 信息来源

- 页面不得重新读取或解释创建表单；
- 书名、篇幅模式、最低章数、目标章数、模型 ID 和内容哈希引用必须来自数据库中的书记录与不可变创建快照；
- 地址、密钥、secret 尾号、故事设想和正文都不进入本页；
- 页面重建后使用 `bookId` 回读，不依赖仅存在于内存的表单对象。

## 2. 信息层级

1. 返回创建页；
2. “开始前确认”标题和已保存说明；
3. 篇幅、章节规模、当前模型三项摘要；
4. 费用状态；
5. 唯一主动作“确认以上信息”；
6. 明确说明当前阶段不会调用模型。

不展示虚构金额、0 元、虚构 token 范围、技术状态码或完成进度。价格目录不可用时，用完整句子说明原因和后续边界。

## 3. 交互

- 主按钮至少 56dp，高频返回目标至少 48dp；
- 点击确认后只提交包含 `bookId`、`snapshotId`、快照内容哈希和价格状态的结构化请求；
- 当前占位阶段确认一次后按钮锁定，并显示“尚未开始生成”的明确结果；
- 系统返回与可见返回按钮行为一致，回到保留状态的创建页；已创建记录不重复写入；
- 加载快照超过一个画面帧时显示进度反馈，不显示空白页；读取失败时提供返回路径，不绕过确认页。

## 4. 视觉与适配

- 继续使用暖纸背景、深墨文字、浅赭容器和橙色主动作，不采用通用 AI 紫色或渐变；
- 使用原生 Material 组件、语义色与文本层级，不用 emoji 代替图标；
- 单一纵向滚动区，最大内容宽度 600dp，支持 375dp、横屏、200% 字体和浅深主题；
- 状态不能只依赖颜色，必须有清晰标题和说明文字；
- 不使用装饰性循环动画，只有实际加载时显示原生进度反馈。

## 5. 验收证据

- API 35 标准竖屏截图：`D:\gptuser\cache\temp\zhijuan-task037-cost-confirmation.png`；
- 已人工检查：中篇 300 章/最低 300 章、冻结模型和未知费用说明清晰；主动作与不调用模型说明均在首屏可达；未显示密钥、地址、快照 ID 或哈希。
